
Tutorial for building the application:
https://scotch.io/tutorials/build-a-restful-api-using-node-and-express-4

https://docs.mongodb.org/manual/reference/mongo-shell/

MongoDB commands:
mongod --dbpath .../data
mongo

// in mongo console
// select db
use nodemath

// insert 1 record
db.usercollection.insert({"operation_id" : "1", "operation" : "add"})
db.usercollection.insert({"_id" : "1", "operation" : "add"})
db.usercollection.find().pretty()

// insert array of records
opdata = [{"_id" : "2", "operation" : "subtract"}, {"_id" : "3", "operation" : "multiply"}, {"_id" : "4", "operation" : "divide"}]
db.usercollection.insert(opdata);

// list items from operations schema
db.operations.find()


Promises:
https://www.promisejs.org
